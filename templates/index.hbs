import { App } from 'vue'
import {{hump name}} from './src/index.vue'

{{hump name}}.install = (app: App) => {
  app.component({{hump name}}.name, {{hump name}})
  return app
}

export default {{hump name}}